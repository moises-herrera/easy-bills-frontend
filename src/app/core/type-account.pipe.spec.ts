import { TypeAccountPipe } from './type-account.pipe';

describe('TypeAccountPipe', () => {
  it('create an instance', () => {
    const pipe = new TypeAccountPipe();
    expect(pipe).toBeTruthy();
  });
});
